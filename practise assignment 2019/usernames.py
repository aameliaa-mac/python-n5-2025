import random

endings = ['ing','end','axe','gex','goh']

number = input("how many usernames should be made? ")

for counter in range (number):
    three = input("first three letters of students name: ")
    length = len(three)
    if length 
    end = random.randstr(endings)
